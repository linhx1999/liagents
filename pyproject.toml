[project]
name = "liagents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "linhx", email = "linhx1999@163.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai<2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "black>=26.1.0",
]
